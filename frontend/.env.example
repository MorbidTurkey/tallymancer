# Copy to .env.local for local dev overrides.
# Leave VITE_API_URL unset when using the Vite dev-server proxy (recommended).
# Set it to the full backend URL when the frontend is hosted separately (e.g. Netlify).
#
# VITE_API_URL=https://tallymancer.com
